import styled from "styled-components";

const Stack = styled.div(({ theme }) => ({
    display: "flex",
    flexDirection: "row",
    alignItems: "center",    
    // height: 'fit-content'
}));

export default Stack;
